# 배포 가이드 (Deployment Guide)

이 문서는 "Legato OCR Excel Converter" 앱을 다른 기기(스마트폰, 다른 컴퓨터)에서 사용할 수 있도록 배포하는 방법을 설명합니다.

---

## ☁️ 옵션 1: Streamlit Community Cloud (추천)
인터넷이 연결된 모든 기기에서 접속할 수 있는 가장 쉬운 방법입니다.

### 1단계: GitHub에 코드 올리기
(이미 완료되었습니다. https://github.com/jangddae83-design/Legato-OCR-Exel-converter)

### 2단계: Streamlit Cloud 연결
1. [Streamlit Community Cloud](https://share.streamlit.io/)에 접속하여 로그인합니다 (GitHub 계정 사용).
2. 우측 상단의 **Use existing repo** (또는 'New app') 버튼을 클릭합니다.
3. 다음 정보를 입력합니다:
    - **Repository**: `jangddae83-design/Legato-OCR-Exel-converter`
    - **Branch**: `main`
    - **Main file path**: `src/app.py`
4. **Deploy!** 버튼을 클릭합니다.

### 3단계: API 키 설정 (중요)
앱이 실행되다가 오류가 날 수 있습니다. API 키 설정이 필요합니다.

1. 배포된 앱 화면 우측 하단의 **Manage app** (또는 'Settings')을 클릭합니다.
2. 세로 점 3개 메뉴(⋮) -> **Settings** -> **Secrets** 탭으로 이동합니다.
3. 아래 내용을 복사하여 붙여넣고 **Save**를 누릅니다.
    ```toml
    GEMINI_API_KEY = "여기에_당신의_API_키를_입력하세요"
    GEMINI_MODEL_NAME = "gemini-3-pro-preview"
    ```
4. 앱을 **Reboot** 하거나 화면을 새로고침하면 정상 작동합니다.

### 4단계: 공유하기
상단 주소창의 URL을 복사해서 스마트폰이나 다른 사람에게 보내주면 바로 사용할 수 있습니다!

---

## 🏠 옵션 2: 로컬 네트워크 (Wi-Fi)
같은 와이파이에 연결된 집/사무실 내의 기기에서만 접속하는 방법입니다.

### 1단계: 내 컴퓨터 IP 주소 확인
터미널에서 다음 명령어를 실행하여 IP 주소를 확인합니다.
- **Mac**: `ipconfig getifaddr en0` (또는 시스템 설정 -> Wi-Fi -> 세부사항)

### 2단계: 앱 실행
터미널에서 앱을 실행합니다.
```bash
streamlit run src/app.py
```

### 3단계: 접속하기
스마트폰 브라우저 주소창에 `http://[내_IP_주소]:8501`을 입력합니다.
예: `http://192.168.0.15:8501`

> **주의:** 컴퓨터 방화벽 설정에 따라 접속이 차단될 수 있습니다. 이 경우 방화벽 설정을 확인해야 합니다.
